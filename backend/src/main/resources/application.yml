server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://ep-tiny-wind-adcromso.c-2.us-east-1.aws.neon.tech/tenaco?sslmode=require
    username: neondb_owner
    password: npg_4KIAYLEWRme7
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1

  sql:
    init:
      mode: always

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    defer-datasource-initialization: true

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  jwt:
    secret: tenaco-jwt-secret-key-that-is-at-least-256-bits-long-for-hs256-algorithm
    expiration-ms: 86400000  # 24 hours

  cors:
    allowed-origins: http://localhost:3000

  upload:
    dir: ./uploads
